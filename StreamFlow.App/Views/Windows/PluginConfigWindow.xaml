<Window x:Class="StreamFlow.App.Views.Windows.PluginConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        Title="Plugin Configuration"
        Height="450" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static adonisUi:Brushes.Layer1BackgroundBrush}}"
        Foreground="{DynamicResource {x:Static adonisUi:Brushes.ForegroundBrush}}">
    <Border Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="16" FontWeight="Bold" Margin="0,0,0,12"/>

            <ContentControl x:Name="ConfigContentControl" Grid.Row="1" Margin="0,0,0,12"/>

            <Button Grid.Row="2" Content="Close" HorizontalAlignment="Right" Width="90" Height="30" Click="OnCloseClick"/>
        </Grid>
    </Border>
</Window>
