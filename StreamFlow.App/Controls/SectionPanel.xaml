<HeaderedContentControl
    x:Class="StreamFlow.App.Controls.SectionPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:StreamFlow.App.Converter"
    xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
    xmlns:local="clr-namespace:StreamFlow.App.Controls"
    HorizontalContentAlignment="Stretch">

    <HeaderedContentControl.Template>
        <ControlTemplate TargetType="{x:Type local:SectionPanel}">
            <Border
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                Padding="{TemplateBinding Padding}"
                CornerRadius="8"
                Margin="0,0,0,8"
                HorizontalAlignment="Stretch">

                <Border.Effect>
                    <DropShadowEffect
                        BlurRadius="15"
                        Color="#000000"
                        Opacity="0.1"
                        ShadowDepth="3"/>
                </Border.Effect>

                <Expander IsExpanded="True"
                          Header="{TemplateBinding Header}"
                          Content="{TemplateBinding Content}"
                          ContentTemplate="{TemplateBinding ContentTemplate}"
                          Background="Transparent"
                          BorderThickness="0"
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch">
                    <Expander.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" Style="{StaticResource PanelHeaderStyle}" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </Expander.HeaderTemplate>
                </Expander>
            </Border>
        </ControlTemplate>
    </HeaderedContentControl.Template>
</HeaderedContentControl>
