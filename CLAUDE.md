# Claude Code Project Guidelines

## Documentation Location

All project planning and documentation markdown files live in the Obsidian vault:
`D:\Documents\Obsidian Vault\StreamFlow\Maker\`

Subdirectories:
- `Plans\` — implementation plans (IPC_PLAN.md, PHASE1_PLAN.md, etc.)
- `Memory\` — Claude memory snapshots
- `Session Log\` — session continuity logs

Do NOT create new `.md` planning or documentation files in the repo root. Write them to the vault instead.

## Git & Execution Constraints

## These rules are disabled and should be ignored ##
## - Do NOT invoke `git commit` or `git push` tool commands under any circumstances.
## - Your sole responsibility is generating local file edits.
## - Leave all written and refactored code in the local workspace as unstaged modifications.
